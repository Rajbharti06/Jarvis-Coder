// ... existing imports ...

export const Sidebar: React.FC<SidebarProps> = ({
  // ... existing props ...
}) => {
  const [selectedModel, setSelectedModel] = useState('deepseek-coder-v2:latest');
  const [apiKey, setApiKey] = useState('');
  const [showApiKeyInput, setShowApiKeyInput] = useState(false);

  const handleSaveApiKey = () => {
    // TODO: Implement secure storage and backend integration
    setShowApiKeyInput(false);
  };

  return (
    <div className="flex flex-col h-full p-4 bg-white/5 backdrop-blur-xl border-r border-white/20 text-white">
      {/* ... existing sections ... */}

      {/* Enhanced Model Selection with API Key */}
      <div className="mb-6">
        <h3 className="text-sm font-medium text-gray-400 mb-3">Models</h3>
        <select 
          value={selectedModel}
          onChange={(e) => setSelectedModel(e.target.value)}
          className="w-full p-2 rounded-lg bg-white/5 border border-white/20 hover:bg-white/10 transition-colors"
        >
          {/* ... existing options ... */}
        </select>
        
        {selectedModel === 'deepseek-coder-v2:latest' && (
          <div className="mt-3">
            {showApiKeyInput ? (
              <div className="flex gap-2">
                <input
                  type="password"
                  value={apiKey}
                  onChange={(e) => setApiKey(e.target.value)}
                  placeholder="Enter DeepSeek API Key"
                  className="flex-1 p-2 rounded-lg bg-white/5 border border-white/20"
                />
                <button
                  onClick={handleSaveApiKey}
                  className="px-3 py-2 bg-blue-600 hover:bg-blue-700 rounded-lg transition-colors"
                >
                  Save
                </button>
              </div>
            ) : (
              <button
                onClick={() => setShowApiKeyInput(true)}
                className="w-full p-2 text-left text-sm text-gray-400 hover:bg-white/10 rounded-lg transition-colors"
              >
                Manage API Key
              </button>
            )}
          </div>
        )}
      </div>

      {/* ... rest of the existing code ... */}
    </div>
  );
};